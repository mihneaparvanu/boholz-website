---
interface Props {
  heading: string;
  highlightWord?: string;
  subheading: string;
}

const { heading, subheading, highlightWord } = Astro.props;
---

<section class="section">
  <div class="heading-group">
    <h2>{heading} <span>{highlightWord}</span></h2>
    <p>{subheading}</p>
  </div>
  <slot />
</section>
<style>
  .section {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-block: var(--spacing-6);
    gap: var(--spacing-5);
  }

  .heading-group {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-3);
    text-align: center;
    width: 100%;
    max-width: 78rem;
    margin-inline: auto;

    h2 {
      font-weight: 500;
    }

    span {
      font-family: var(--font-secondary);
      font-style: italic;
      font-weight: 500;
    }

    p {
      color: var(--clr-content-secondary);
      max-width: 56rem;
      font-weight: 400;
    }
  }
</style>
