# /dev/sandbox — UI reference pages

These used to be live at `/sandbox/*` and `/ui-preview`. Moved out of
`src/pages/` to remove them from production routing.

To view one locally: copy the `.astro` file into `src/pages/`, run
`bun run dev`, visit the URL, then delete the copy. Never commit it back
into `src/pages/`.
